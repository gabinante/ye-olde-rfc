[project]
name = "olde-rfc"
version = "0.1.0"
description = "Transform technical specs into medieval manuscripts"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "jinja2>=3.1",
]

[project.optional-dependencies]
pdf = ["weasyprint>=62.0"]

[project.scripts]
olde-rfc = "olde_rfc:main"
